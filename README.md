# udacity_repo
This is a repository to gain familiarity with git and Github.

### Git Commands
* git clone
* git checkout -b
* git checkout
* git branch
* git branch -d
* git add
* git commit -m
* git push
